INSTRUÇÕES PARA EXECUÇÃO DO SISTEMA

Requisitos: Utilizar o Docker.

Passo 1 (Subir os serviços): Abra o terminal na raiz do projeto (onde está o docker-compose.yml) e rode:

docker compose up -d --build

Passo 2 (Banco de dados): A base é inicializada automaticamente usando o arquivo www/cadastros.sql.

Passo 3 (Acesso): Abra o navegador em http://localhost:8080.

Passo 4 (Login): Utilize o e-mail e a senha cadastrados na tabela usuarios.

arnholdthaila92@gmail.com
54321

Passo 4: Acesse os serviços.